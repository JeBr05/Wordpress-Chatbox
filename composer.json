{
  "name": "open-source-contributors/jeroens-chatbox",
  "description": "WordPress plugin for an OpenAI powered knowledge base chatbox.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.0"
  },
  "scripts": {
    "lint": "find . -name '*.php' -print0 | xargs -0 -n1 php -l"
  }
}
